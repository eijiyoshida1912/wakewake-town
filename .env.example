# ランキングAPI（Cloudflare Worker）のURL。デプロイ手順は worker/README.md を参照。
# 未設定のままでもアプリは動く（ランキング画面が「まだ準備中だよ」の表示になるだけ）。
# コピーして .env.local を作り、実際のURLを書く（.env.local はコミットしない）。
NEXT_PUBLIC_RANKING_API_URL=http://localhost:8787
